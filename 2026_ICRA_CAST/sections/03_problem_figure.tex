% Compact single-column motivation, introduced at its first reference.
\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/problem_motivation.pdf}
  \caption{\textbf{Same critic, different policy update.}
  Both panels start from the same blue base distribution and $384$ paired
  actions. (a) Vanilla critic ascent moves probability mass into the
  overestimated region (gray). (b) Anchoring permits improvement near the
  base. Colored contours enclose $50/80/95\%$ of the estimated mass;
  paths show representative updates in shared, rotated action coordinates.
  Scores are particle averages. \hl{superfluous: This single-seed toy uses $25$ critic-training
  samples, $60$ updates, and a $0.06$ step cap. Radius-$0.15$ per-particle
  projection provides a fixed-reference diagnostic; CAST uses soft
  restoration during policy training}.\hl{needs a connection to robotics}}
  \label{fig:problem}
\end{figure}
