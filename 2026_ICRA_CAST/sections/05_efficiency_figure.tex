\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/diffusion_efficiency.pdf}
\caption{\textbf{CAST reaches 90\% success sooner.}
On robomimic square, circles mark the first evaluated checkpoint reaching
90\% success in each of three training runs from a shared diffusion base;
diamonds mark the means. CAST requires 38\% fewer steps on average.
The two CAST runs reaching the threshold at 160K steps are offset
vertically for visibility. Final success at 496K steps is similar:
CAST 95.9\%, DICE-RL 96.5\%.}
\label{fig:diffusion-efficiency}
\end{figure}
