\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/real_robot_tasks.pdf}
  \caption{OpenArm tasks and autonomous success rates (\%).
  The first four panels in each row illustrate a successful teleoperated
  demonstration: initial state, grasp, alignment, and release. Views alternate
  between chest and wrist cameras; times are nominal at 15 fps.
  The final panel reports separate autonomous evaluations, with 20 attempts
  per method and successful attempts/total above each bar. Base is the
  pretrained flow-matching policy, BC is supervised refinement, and DICE
  and +CAST denote DICE-RL and CAST. The two tasks share the same
  percentage scale; bold counts identify the highest observed success.
  Placement poses are randomized on the stairs.}
  \label{fig:real-robot}
\end{figure*}
