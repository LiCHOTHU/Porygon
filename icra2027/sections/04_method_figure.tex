\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/method_update.pdf}
  \caption{\textbf{CAST constructs a target before updating the actor.}
  Read clockwise: (1) sample paired base and current actions; (2) propose
  a clipped critic correction; (3) add restoration and clip the combined
  displacement; (4) fit the residual to fixed targets. All panels share
  the same state $s$, coordinate scale, and base/current distributions.
  Here $\bar r_i=\operatorname{sg}[r_\theta(s,z_i)]$ and the action target
  is $\tilde a_i=a_{0,i}+\tilde r_i$; $\operatorname{sg}$ stops gradients.
  In step 2, the dashed arrow continues the uncapped request
  $\eta_Qg_i$ beyond the view. In step 3, restoration is evaluated at the
  current residual and drawn head-to-tail to show addition. The dashed
  gray arrow shows total-step clipping; the short green arrow is the
  accepted correction $\Delta_i$. The dotted circle has radius
  $\delta_{\mathrm{tot}}$ around the current action.
  Step 4 shows the per-state regression objective from
  \eqref{eq:actor-regression}. This is a constructed schematic with
  illustrative parameters; green denotes targets rather than a guaranteed
  post-update policy.}
  \label{fig:method-update}
\end{figure}
