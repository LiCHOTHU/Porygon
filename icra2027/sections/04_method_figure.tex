\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/method_update.pdf}
  \caption{\textbf{One coherent CAST update, read clockwise.}
  The blue base and gray current-actor distributions are identical in all
  four panels. The orange proposal from step 2 carries into step 3; the
  corrected green targets carry into step 4 for regression. Here
  $a_{0,i}=\pi_0(s,z_i)$ and $g_i=\nabla_a\bar Q_\phi/q_{\mathrm{scale}}$.
  This constructed schematic follows
  \eqref{eq:candidates}--\eqref{eq:actor-regression} with illustrative
  parameters and a shared coordinate scale. The dashed raw request in step 2
  continues off-panel. The green cloud denotes fixed targets, not a
  guaranteed post-update policy.}
  \label{fig:method-update}
\end{figure}
