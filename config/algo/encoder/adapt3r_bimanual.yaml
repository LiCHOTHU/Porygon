defaults:
  - adapt3r
  - _self_

_target_: imitation.algos.encoders.adapt3r_bimanual.BimanualAdapt3REncoder
do_hand_crop: false
bimanual_mode: concat
do_lang: false
# backbone_type: clip
# pointcloud_extractor_factory:
#   _target_: imitation.algos.utils.pointnet_extractor.PointNetEncoder
#   _partial_: true
#   out_channels: ${algo.embed_dim}
#   use_layernorm: false
#   final_norm: "none"
#   block_channel: [256, 256, 256, 512]
#   reduction: attention
# lowdim_encoder_factory:
#   _target_: imitation.algos.utils.mlp_proj.MLPProj
#   _partial_: true
#   output_size: ${algo.lowdim_embed_dim}
#   num_layers: 1
# num_points: 512
# hidden_dim: 60
# task_suite_name: ${task.suite_name}
# task_benchmark_name: ${task.benchmark_name}
# finetune: false
# hand_frame: true
# do_crop: true
# tight_crop: true
# do_hand_crop: true
# downsample_mode: feat
# xyz_proj_type: nerf
# do_image: true
# do_lang: true
# use_old_hand_frame_crop: false


# load_image: true
# load_depth: true