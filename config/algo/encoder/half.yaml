defaults:
  - base
  - _self_

_target_: imitation.algos.encoders.half.HalfEncoder
finetune: false
lowdim_encoder_factory:
  _target_: imitation.algos.utils.mlp_proj.MLPProj
  _partial_: true
  output_size: ${algo.lowdim_embed_dim}
  num_layers: 1


load_image: true
load_depth: true