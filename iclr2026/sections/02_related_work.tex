% Buckets (citation map verified 2026-07; see reference_motivation_citations):
% 1. RL fine-tuning of generative policies: DICE-RL (arXiv 2603.10263), SimpleVLA-RL,
%    DSRL, DPPO-style diffusion PG; likelihood intractability for one-step models.
% 2. Drifting / one-step generative policies; DFP (arXiv 2605.07727) = RL on a
%    drifting base — differentiate: DFP ranks with the critic inside a drift loss;
%    we unify BOTH reward and anchor as velocity fields with an explicit trust region.
% 3. Residual RL: ResFit/act_sim, DAWN (warmup buffer + critic LayerNorm) — DAWN is
%    the strong residual baseline.
% 4. Critic exploitation / conservatism: CQL etc.; diversity collapse in RL-finetuned
%    generators; tilted-distribution transport precedents (AWR-style exp weighting).
\todo{Related work.}
