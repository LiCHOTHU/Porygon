\todo{Code release (training framework + official-harness implant), exact
configs (family-shared recipe), seeds, eval protocols (300-ep robomimic;
100$\times$3 powered LIBERO), compute (single L40S/A100 per run, 8--20h),
toy experiment script. All numbers in \cref{tab:robomimic} are averages,
never best-of.}
