% Numbers policy: a cell is filled ONLY with a completed, matched-protocol
% measurement (300-ep robomimic / powered LIBERO). "---" = run in progress.
% Provisional values live in DEVLOG.md, not here.

Our experiments answer three questions. \textbf{Q1:} Does the field-target
\emph{form} beat backpropagation given the \emph{same} reward signal and
budget? \textbf{Q2:} Can a one-step drifting base fine-tuned with \method{}
overtake the stronger flow-matching pipeline? \textbf{Q3:} When should the
reward field use the critic's gradient versus only its values --- does the
regime picture of \cref{sec:toy} hold on real benchmarks?

\subsection{Setup}
\label{sec:exp-setup}

\textbf{Benchmarks.} (i) \emph{robomimic} can and square (state-based;
300-episode evaluations, average of the last three checkpoints; 3 seeds),
run inside the official DICE-RL harness with only the actor update
replaced --- critic, replay, demonstrations, and evaluation protocol are
byte-identical to the baseline. (ii) The \emph{eight hardest tasks of
LIBERO-90} (multitask drifting base; 100 RL iterations; final numbers use
powered evaluation: 100 rollouts $\times$ 3 evaluation seeds).
\todo{appendix table: env details, horizons, demo counts, budgets.}

\textbf{Compared methods.} Each comparison is chosen to isolate one of the
three questions; every method on the drifting base shares the residual
parameterization, critic, data, and budget, so only the actor update
varies.
\begin{itemize}
  \item \textbf{\method{}}: \cref{eq:update} with the gradient reward
  field $V_Q^{\mathrm{grad}}$.
  \item \textbf{Backprop actor} (the controlled comparison for
  \textbf{Q1}): the DICE-RL residual actor \citep{dicerl2026} ---
  backpropagated $-Q$ with a filtered BC penalty --- run on \emph{our}
  drifting base. It consumes the identical critic through the identical
  residual head, so any gap to \method{} is attributable to the update
  \emph{form}, not to the base, the reward signal, or the data.
  \item \textbf{FM + DICE-RL} (the external reference for \textbf{Q2}):
  the unmodified state-of-the-art pipeline on its own flow-matching base
  \citep{dicerl2026}. It answers whether the one-step base plus \method{}
  competes with the strongest published system, not merely with its own
  control.
  \item \textbf{\method{} (top-$k$)} and \textbf{\method{} (tilted)}
  (resolution ablations for \textbf{Q3}, not baselines): the same update
  with the reward field built from the critic's \emph{rankings}
  (transport toward the top-$k$ particles) or its \emph{soft values}
  ($\exp(Q/\tau)$-weighted self-attraction). Each deletes the critic's
  gradient direction while changing nothing else, measuring how much of
  \method{}'s gain flows through that one channel --- and, in the
  weak-critic LIBERO regime, whether the gradient is worth consuming at
  all.
\end{itemize}
Hyperparameters are \emph{family-shared} across \method{} and its two
resolution variants ($\eta_Q{=}0.5$,
$\delta_{\mathrm{tot}}{=}0.15$, $\eta_{\mathrm{anc}}{=}1.0$, $\tau{=}1$,
$k{=}4$; dead zone $\rho{=}0.05$ on LIBERO) so that comparisons isolate
the update rule rather than tuning effort.

\subsection{Robomimic: the gradient regime}
\label{sec:exp-robomimic}

\begin{table}[t]
\centering
\caption{robomimic success rate (300-episode evaluations, last-3-checkpoint
average, per seed). Bold marks the best value in each column (ties within
$0.005$). ``---'' = matched-budget run still in progress at
submission-draft time; no provisional numbers are shown.}
\label{tab:robomimic}
\small
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{3}{c}{can} & \multicolumn{3}{c}{square} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
method & s42 & s43 & s44 & s42 & s43 & s44 \\
\midrule
drift base (no RL)         & \multicolumn{3}{c}{0.877} & \multicolumn{3}{c}{0.382} \\
backprop actor (same base) & --- & --- & 0.940 & 0.870 & 0.909 & 0.861 \\
FM + DICE-RL               & \textbf{0.989} & 0.979 & \textbf{0.986} & \textbf{0.934} & 0.923 & \textbf{0.930} \\
\method{} (top-$k$)        & 0.924 & 0.920 & 0.910 & 0.562 & 0.525 & 0.562 \\
\method{} (tilted)         & 0.949 & 0.947 & 0.919 & 0.578 & 0.584 & 0.560 \\
\method{}                  & \textbf{0.988} & \textbf{0.993} & 0.978 & 0.912 & \textbf{0.930} & \textbf{0.929} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/robomimic_curves.pdf}
  \caption{robomimic learning curves (300-episode evaluations every 1K
  steps; mean and min--max band over seeds; dashed vertical line marks the
  matched 20K budget). On square, \method{} overtakes the backprop actor
  by 7K steps and holds a $4$--$7$-point lead through the
  matched budget; the value-only variants (top-$k$, tilted) flatline near
  $0.55$. On can,
  \method{} and the flow-matching pipeline both approach the ceiling
  ($\geq 0.98$). \todo{regenerate with the s44 and can-baseline reruns
  when they finish.}}
  \label{fig:curves}
\end{figure}

\Cref{tab:robomimic} gives the per-seed results, and \cref{fig:curves}
the corresponding learning curves. On can, fine-tuning saturates the task:
\method{} reaches $0.978$--$0.993$ from a base at $0.877$, the
flow-matching pipeline reaches $0.979$--$0.989$, and the backprop actor
trails slightly at $0.940$ --- the can task chiefly certifies
that the field update preserves (and slightly extends) the base's
competence at the ceiling. The discriminative task is square, where the drifting base solves
only $0.382$ of episodes; \method{} adds $55$ points, reaching
$0.91$--$0.93$ on every seed. The backprop actor --- same base,
same critic, same data, same budget --- reaches $0.86$--$0.91$
(\textbf{Q1}): given the identical reward signal, the field-target form
extracts strictly more progress at the matched budget. Against the
flow-matching pipeline, \method{} reaches parity on both tasks
($0.91$--$0.93$ vs.\ $0.92$--$0.93$ on square; $0.99$ vs.\ $0.99$ on can)
--- but from a base less than half as strong on square (\textbf{Q2}): the
safer update recovers the headroom the weaker base gives up.

\emph{Why does the form win?} The trust region converts critic error
from a stability threat into bounded noise: with policy motion capped at
$\delta_{\mathrm{tot}}$ per update, \method{} tolerates a reward step
($\eta_Q{=}0.5$) far above what the backprop actor can survive,
and simply takes more, safer steps in the directions where the critic is
consistent. The backprop actor must instead keep its effective step small
enough for its \emph{worst} critic errors, and it pays in speed:
\method{} reaches $0.91$ on every seed by $15$K steps --- a level the
backprop actor needs another $5$--$10$K steps to reach. Its
matched-budget endpoint trails \method{}'s on every seed, and the gap
closes only near $40$K, where both converge to $0.97$--$0.99$. The form's advantage on this benchmark is therefore a
faster, never-worse path to the same asymptote --- exactly what a
bounded-step update should buy \todo{quantify further with the
stability-window ablation (\cref{sec:exp-ablation}, iii)}.

The dial answers \textbf{Q3} in its first regime: with a critic that
generalizes (robomimic critics see dense, single-task data), the value-only
reward fields preserve can ($0.91$--$0.95$) but collapse on square
($0.53$--$0.58$ versus \method{}'s $0.91$--$0.93$), despite
verified-healthy residuals and longer training. Deleting the gradient
direction throws away exactly the
signal that solves the harder task --- the real-benchmark mirror of the
toy's easy regime, where the compass wins.

\subsection{LIBERO hard-8: the weak-critic regime}
\label{sec:exp-libero}

On the eight hardest tasks of LIBERO-90 we deploy \method{} as a
\emph{routing system}: one frozen multitask drifting base, one small
per-task residual head and critic fine-tuned by \method{} on each task's
own interactions, and task-ID routing at deployment --- the architecture
every published LIBERO RL fine-tuner effectively uses. One recipe is
shared across all tasks with no per-task tuning ($\eta_Q{=}1.0$,
dead-zone anchor with a dimension-independent rms radius $\rho{=}0.05$,
$3$-step returns, $40$ iterations of $16$ episodes).

\begin{table}[t]
\centering
\caption{LIBERO hard-8, per-task fine-tuning from the shared drifting
base (success under powered evaluation: $100$ rollouts $\times$ $3$
evaluation seeds; \method{} rows average the last three training
checkpoints). One recipe across all tasks. ``---'' = run still in
progress at draft time.}
\label{tab:libero-single}
\small
\setlength{\tabcolsep}{4.2pt}
\begin{tabular}{lccccccccc}
\toprule
 & t8 & t21 & t32 & t53 & t65 & t73 & t75 & t81 & mean \\
\midrule
drift base (no RL) & 0.818 & 0.690 & 0.610 & 0.785 & 0.573 & 0.648 & 0.578 & 0.588 & 0.661 \\
backprop actor     & 0.820 & 0.703 & 0.587 & 0.800 & 0.550 & 0.663 & 0.583 & 0.653 & 0.670 \\
\method{}          & \textbf{0.831} & \textbf{0.737} & \textbf{0.684} & \textbf{0.844} & \textbf{0.781} & \textbf{0.710} & \textbf{0.657} & 0.607 & \textbf{0.731} \\
\midrule
\multicolumn{10}{l}{\emph{same protocol, flow-matching base (4 tasks measured)}} \\
FM base (no RL)    & --- & --- & 0.690 & 0.917 & 0.787 & --- & 0.620 & --- & --- \\
FM + DICE-RL       & --- & --- & 0.682 & 0.915 & 0.809 & --- & 0.592 & --- & --- \\
FM + \method{}     & --- & --- & \textbf{0.738} & --- & \textbf{0.878} & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:libero-single} gives the verdict across all eight tasks:
\method{} gains $+1.3$ to $+20.8$ points per task (mean $+7.0$),
improving on the base for \emph{every} task --- on the highest-headroom task it reaches $0.781$,
above the best previously recorded result on that task by any method ---
while the backpropagated actor consuming the \emph{same} per-task
critics averages $+0.9$, going \emph{below base} on the two
highest-headroom tasks. The robomimic ordering (\textbf{Q1}) transfers
intact to the harder benchmark: identical reward signal, and only the
field-target form converts it into gains without ever falling below the
base. Two implementation findings were load-bearing and are ablated in
\cref{sec:exp-ablation}: the anchor's dead zone must be defined in
dimension-independent rms units (the vector-norm form silently
re-activates the anchor at LIBERO's $S{=}112$ chunk dimension and
freezes the residual), and the weaker critic gradient
($\|\nabla_a Q\|$ roughly $4\times$ smaller than robomimic's) requires
the doubled reward step the trust region makes safe.

The flow-matching rows settle \textbf{Q1} independently of the base. On
its own, stronger base the state-of-the-art pipeline extracts essentially
nothing per task: FM + DICE-RL scores $-0.8$, $-0.2$, $+2.2$ and $-2.8$
points relative to the FM base on t32, t53, t65 and t75 (mean $-0.4$),
the same near-zero extraction its actor shows on the drifting base
($+0.9$). Swapping only the actor update fixes it: \method{} on the
\emph{identical} flow-matching base and budget reaches $0.738$ on t32 and
$0.878$ on t65 --- $+4.8$ and $+9.1$ over that base, and $+5.6$ and
$+6.9$ over FM + DICE-RL on the same two tasks. The field-target form,
not the base and not the reward signal, is what converts critic signal
into success. It also puts the \textbf{Q2} comparison in perspective:
\method{} on the weak one-step base reaches $0.781$ on t65 against the
flow-matching pipeline's $0.809$ --- within three points, from a base
$21$ points lower --- and \emph{exceeds} it on t75 ($0.657$ vs.\ $0.592$)
and on t32 once the reward step is matched to that task's stronger critic
gradient ($0.729$ vs.\ $0.682$).
\todo{per-task figure; remaining FM cells and matched-budget FM + GRPO.}

\paragraph{Why not one shared critic? The multitask control.}
The alternative to routing is a single policy and critic trained on all
eight tasks jointly. This setting flips the critic's situation: our
diagnostics show the shared critic's action-gradient collapses on most
tasks (per-task $\|\nabla_a Q\|$ spanning $0.003$ to $0.55$), and the
toy predicts the resolution dial should compress.

\begin{table}[t]
\centering
\caption{LIBERO hard-8 mean success under powered evaluation ($100$
rollouts $\times$ $3$ evaluation seeds per cell; one column per training
seed; $100$ RL iterations). Neither base is moved by \emph{any}
fine-tuner: the weak multitask critic supports no extractable gain, for
backpropagation and field transport alike. ``---'' = evaluation still
running.}
\label{tab:libero}
\small
\begin{tabular}{lccc}
\toprule
method & s10000 & s10001 & s10002 \\
\midrule
drift base (no RL)         & \multicolumn{3}{c}{$0.671 \pm 0.014$} \\
backprop actor (same base) & 0.657 & 0.655 & 0.668 \\
\method{} (top-$k$)        & --- & 0.684 & --- \\
\method{} (tilted)         & 0.677 & --- & 0.672 \\
\method{}                  & 0.655 & --- & 0.659 \\
\midrule
flow-matching base (no RL) & \multicolumn{3}{c}{$0.738 \pm 0.022$} \\
FM + DICE-RL               & 0.756 & 0.735 & 0.737 \\
\bottomrule
\end{tabular}
\end{table}

The powered numbers (\cref{tab:libero}) deliver a verdict that noisy
small-sample readouts had obscured: \emph{nothing wins here}. On the
drifting base, the backprop actor ($0.655$--$0.668$), \method{}
($0.655$--$0.659$), and every resolution variant ($0.66$--$0.70$) all sit
within noise of the untuned base ($0.671 \pm 0.014$). The flow-matching
pipeline looks stronger in absolute terms ($0.735$--$0.756$) --- but its
own base already scores $0.738 \pm 0.022$, so DICE-RL likewise extracts
essentially nothing; its apparent lead on this benchmark is inherited
entirely from pretraining. Three honest conclusions follow. First, the
weak-critic regime is a \emph{no-gain} regime for every update rule we
tested: when the shared critic's action-signal is this poor, neither
backpropagating it nor transporting along it, at any resolution, converts
it into task success. Second, the dial compresses exactly as the toy
predicts --- gradient, ranking, and soft-value sources become
indistinguishable --- though in the starkest possible form. Third, every
drift-base arm \emph{preserves} the base within noise: the failure mode of
\cref{fig:intuition} does not appear even for the backprop actor,
consistent with the diagnosis that this critic is action-blind rather than
action-exploitable --- there is no steep hallucinated gradient to ride.
Small-sample evaluations told a different, wrong story (single-seed
$20$-episode endpoints ranged $0.63$--$0.78$ across these same arms);
powered evaluation collapses that spread to the base value.
\todo{per-task breakdown figure (\texttt{scripts/plot\_libero\_pertask.py})
once the remaining seed evaluations land; include the guarded BC-filter
interaction in the appendix.}

\subsection{Ablations: which component pays where}
\label{sec:exp-ablation}

\begin{figure}[t]
  \centering
  \figplaceholder{3.6cm}{\textbf{[Figure reserved: two ablation panels.]}
  Left --- \emph{stability window}: final square success vs.\ reward step
  $\eta_Q$ for \method{} and for the backpropagated control (A + gradient
  clipping); the claim is a wide flat optimum for the field form vs.\ a
  narrow peak for backpropagation. Right --- \emph{dispersion retention}:
  action dispersion and best-of-$N$ curve before/after fine-tuning for
  \method{} vs.\ A.}
  \caption{Ablations: where the trust region and update form pay.
  \todo{runs in progress}}
  \label{fig:ablations}
\end{figure}

\begin{table}[t]
\centering
\caption{LIBERO ablations: leave-one-out on the frozen recipe, two tasks,
powered evaluation (last-3 checkpoints). Every row changes exactly one
component. $\Delta$ is relative to the full recipe.}
\label{tab:libero-ablation}
\small
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{t65 (base 0.573)} & \multicolumn{2}{c}{t32 (base 0.610)} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
variant & success & $\Delta$ & success & $\Delta$ \\
\midrule
\textbf{full recipe} & \textbf{0.781} & --- & \textbf{0.684} & --- \\
$-$ dimension-independent anchor (vector-norm $\rho$) & 0.662 & $-0.119$ & 0.655 & $-0.029$ \\
$-$ dead zone ($\rho{=}0$) & 0.549 & $-0.232$ & --- & --- \\
$-$ doubled reward step ($\eta_Q{=}0.5$) & 0.769 & $-0.012$ & 0.615 & $-0.069$ \\
$-$ $3$-step returns ($n{=}1$) & 0.782 & $+0.001$ & 0.512 & $-0.172$ \\
wider trust region ($\delta_{\mathrm{tot}}{=}0.25$) & --- & --- & 0.689 & $+0.005$ \\
stronger reward step ($\eta_Q{=}2.0$) & --- & --- & \textbf{0.729} & $\mathbf{+0.045}$ \\
\midrule
reward field $\to$ top-$k$ (no gradient) & 0.577 & $-0.204$ & --- & --- \\
reward field $\to$ tilted (no gradient) & 0.697 & $-0.084$ & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:libero-ablation} isolates each component. Three findings
matter. First, \emph{the anchor geometry is load-bearing}: removing the
dead zone entirely ($\rho{=}0$) drops t65 to $0.549$, \emph{below} the
un-tuned base --- the un-gated restoring pull erases the residual, the
quantitative version of the toy's ``penalties lose to fields'' lesson ---
and measuring the dead-zone radius in vector-norm rather than
dimension-independent units costs $11.9$ points, because at LIBERO's
$S{=}112$ chunk dimension the base residual already exceeds $\rho$ at
initialisation, so the anchor is active from the first update. Second,
\emph{deleting the critic's gradient direction is fatal here too}:
top-$k$ transport falls to $0.577$ (base level) and tilted transport to
$0.697$, mirroring the robomimic square result and answering
\textbf{Q3} on both benchmarks. Third, the step-size and credit-horizon
components are \emph{task-dependent in exactly the way the critic
diagnostics predict}: on t32, whose critic carries a strong
action-gradient ($\|\nabla_a Q\|\!\approx\!0.55$), $3$-step returns are
worth $17$ points and the doubled step $7$; on t65, whose gradient is an
order of magnitude weaker ($\approx\!0.035$), both are within noise. Pushing the
reward step further on that task ($\eta_Q{=}2.0$) adds another $4.5$
points ($0.729$), while widening the trust region alone
($\delta_{\mathrm{tot}}{=}0.25$) changes nothing ($+0.5$): it is the
\emph{reward} step, not the movement bound, that the strong-gradient task
wants. What a bounded-step field update needs is therefore not one
universal step size but a step matched to the signal the critic actually
carries --- which the trust region is what makes safe to raise.

\todo{Remaining ablation plots (\cref{fig:ablations}); robomimic-side
items below:
(i) \textbf{trust-region locus} --- the backprop actor + parameter-space
gradient
clipping vs.\ B's action-space clip at equal effective step (isolates the
central claim);
(ii) \textbf{anchor geometry} --- $\rho{=}0$ silently erases the residual
under weak reward fields (measured residual RMS collapses $\sim$5$\times$
and performance reverts to base-plus-selection), the quantitative version
of the toy's ``penalties lose to fields'' lesson;
(iii) \textbf{step/clip sweep} --- the 21-configuration square sweep behind the
family recipe: stability window of $\eta_Q$ under \cref{eq:update} vs.\
backpropagation;
(iv) \textbf{dispersion retention} --- action dispersion and best-of-$N$
gains after fine-tuning, vs.\ control (does the field update preserve the
multimodality the base was trained for?).}
